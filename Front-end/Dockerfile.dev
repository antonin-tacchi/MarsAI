FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Copy the docker-specific environment file
RUN cp .env.docker .env.local

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]
